$(document).ready(function() {
	// see accordion in script.js
/*	$('.navigation.accordion > li ul')
		.click(function(e){
			e.stopPropagation();
		})
		.filter(':not(:first)')
		.hide();
	
	$('.navigation.accordion > li').click(function() {
		var selfClick = $(this).find('ul:first').is(':visible');

		if (selfClick){
			return;
		}

		$(this)
			.parent()
			.find('> li ul:visible')
			.slideToggle()

		$(this)
			.parent()
			.children()
			.removeClass('active');

		$(this)
			.addClass('active')
			.find('ul:first')
			.stop(true, true)
			.slideToggle();
	}); */
});
