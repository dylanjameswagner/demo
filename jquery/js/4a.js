$(document).ready(function() {
	// see expandable in script.js
/*	$('.navigation.expandable > li > ul')
		.hide()
		.click(function(e){
			e.stopPropagation();
	});
	$('.navigation.expandable > li').toggle(function() {
		$(this)
			.css('background-position', 'right -58px')
			.find('ul').slideDown();
	}, function() {
		$(this)
			.css('background-position', 'right 3px')
			.find('ul').slideUp();
	});	*/
});
